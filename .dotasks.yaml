version: 1
name: DoTask
description: Portable, cross-platform tasks written in the language of your choice.
settings:
  solution: dotask.slnx
  project: src/Dotask.Cli/Dotask.Cli.csproj
targets:
  dotnet-publish:
    defaults:
      self-contained: true
